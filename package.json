{
  "name": "event-manager",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "8.9.x"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules",
    "server/node_modules"
  ],
  "scripts": {
    "start": "set NODE_ENV=production && node server/src/index.js",
    "install-client": "cd client/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
    "heroku-postbuild": "npm run install-client && cd server/ && npm install && npm install --only=dev --no-shrinkwrap"
  },
  "dependencies": {
    "express": "4.15.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vyacheslavborovsky/event-manager-app.git"
  },
  "license": "MIT",
  "devDependencies": {

  }
}
